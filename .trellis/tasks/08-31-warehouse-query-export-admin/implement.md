# 第 8 项实施与验收计划

2026-09-09：实施及本地自动/浏览器检查完成。用户在验收汇报和提交请求后回复“继续”，确认本地交付及此前的提交方案。最终结果和提交范围见 `research/implementation-evidence-2026-09-09.md`；工作提交、归档及 journal 的实际回执以 Git 和会话记录为准，不在工作提交快照中预先勾选尚未执行的动作。未授权推送、合并或上线。

## Phase 0: Final Planning Gate

- [x] 检查 `main@53a27c4`、第 7 项合并回执和主目录既有改动。
- [x] 用户确认命中明细、双日期口径、24 小时下载有效期、映射只读边界。
- [x] 读取现有查询、权限、发放、worker 和前端规格，形成研究记录与技术设计。
- [x] 整理 PRD，区分确认决定、拟采用资源保护值、非目标和实施授权。
- [x] 向用户提交最终摘要；用户于 2026-09-09 明确批准开始实施。
- [x] 创建 `codex/warehouse-query-export-admin` 独立工作树并复制已批准规划文件；主目录原规划副本和登录延期改动保持原样，实施以独立工作树为准。
- [x] 运行 `task.py start`，回读任务、分支、工作树和上下文；单代理执行，不启动子代理或独立 Codex 任务。

## Phase 1: Contracts and Query

- [x] 读取 trellis-before-dev 和对应规格，新增共享查询、游标、人员、导出任务响应契约及错误码。
- [x] 编写统一权限仓库范围、上海日期、同一明细 AND 谓词、筛选哈希和投影。
- [x] 实现分页业务查询及授权范围历史领用人候选，覆盖停用人员/产品与空结果。
- [x] 增加详情库存流水分页投影，复用既有整单详情和权限规则。
- [x] 使用 PostgreSQL 合成样本验证混领过滤、跨日、重提交、补手续不改变发放时间、越权和稳定分页。

回退点：不修改原审核队列行为，不回填 Request/Fulfillment/InventoryMovement 日期。

## Phase 2: Persistent Export Jobs

- [x] 核对下一个 migration 编号，仅增加导出任务表和查询索引。
- [x] POST 创建复用幂等执行器，事务化权限、每人未完成数量限制和审计。
- [x] 实现任务列表/详情、SKIP LOCKED 领取、租约心跳、失效恢复、尝试上限和稳定错误。
- [x] 验证并发同 key、不同条件同 key、两 worker 争抢、用户撤权、任务失败/过期以及重启恢复。

回退点：只操作专门的合成测试库，不改 `warehouse_feishu_shadow` 的数据和绑定。

## Phase 3: XLSX and Private Storage

- [x] 安装固定 `exceljs@4.4.0` 到 worker，检查许可证、锁文件、依赖审计及 Node 24 ESM/TS 实际兼容。
- [x] 新增独立 exporter 入口及启动命令，避免插入会阻塞 outbox 的串行任务。
- [x] 在同一有超时快照中按顺序写两个工作表，分批读取并逐行释放，不缓存在 API 内存。
- [x] 实现私有目录、唯一尝试文件、原子发布、大小/行数/时间限制、哈希和失败清理。
- [x] 实现 24 小时到期及每 5 分钟补偿清理，清理绝对路径约束和孤儿文件策略。
- [x] 增加受保护流式下载，覆盖所有权、当前角色/仓库、到期、文件丢失、路径/符号链接和下载失败。
- [x] 用真实生成的 XLSX 回读两表、行数、数字类型、文本安全、空表和汇总一致性。

若固定依赖无法满足兼容/安全要求，记录证据并回到设计；不得换库后沿用未重跑的绿灯。

## Phase 4: Administration and UI

- [x] 完成人员查询端点，白名单输出；复用权限更新、最后管理员保护和原子审计。
- [x] 增加查询/导出、人员权限页面，复用 shadcn-vue、Lucide 和既有状态组件。
- [x] 接入导航与授权回跳，保留现有审核、商品、日历、待办和同步管理页面。
- [x] 实现异步任务轮询、页面离开/恢复、过期/失败/下载错误反馈及未知提交复用 key。
- [x] 越权/撤权清空敏感页面状态，取消无效请求，不让晚到响应恢复已撤权信息。

## Phase 5: Full Verification

- [x] 单元/HTTP：输入边界、日期、同一明细条件、文件投影、稳定错误、四类角色/仓库矩阵、CSRF 和幂等。
- [x] PostgreSQL：一致快照、查询索引、并发生成/领取/撤权/最后管理员、租约发布和崩溃恢复。
- [x] 合成 100,000 明细压测：两表数值回读、单任务不超过 300 秒、文件不超过 64 MiB；记录 exporter RSS，目标不超过 512 MiB。
- [x] 同时压测独立 API：固定本地条件下 20 并发普通查询，无失败，p95 不超过 1 秒；记录基线和有导出负载结果。未达目标先分析，不静默降低规模或吞掉样本。
- [x] 停用 exporter 及存储不可用的负例给出可理解失败，API 和库存业务保持可用；生产目录 ACL 留上线验收。
- [x] 浏览器 1440x900 / 390x844：筛选、分页返回、双日期、混领、权限弹窗、长文本、空/错误/过期、撤权及下载。
- [x] 下载至少一份经过真实本地 API/exporter/测试数据库生成的文件并回读；模拟登录仅用于隔离 QA，不声称生产 OAuth 已验收。
- [x] fake gateway 回归原同步重试、对账及只读映射；不进行真实飞书调用。
- [x] 执行最终完整检查：

```bash
pnpm db:generate
pnpm --filter @glorychips/database exec prisma validate --config prisma.config.ts
pnpm lint
pnpm typecheck
pnpm test
pnpm test:integration
pnpm build
pnpm format:check
git diff --check
```

## Phase 6: Delivery

- [x] 更新实际落地的 backend/frontend spec，研究结论不能冒充已实现契约。
- [x] 最终复核及 67 文件范围分类已完成，用户确认本地交付并批准提交计划。
- [ ] 获准后工作提交 -> archive -> journal；记录质量结果和未验收边界，不 amend 或混入他人文件。
- [ ] 推送、PR 合并、临时工作树移除分别取得明确授权。
- [ ] 第 9 项仅在第 8 项交付完成后进入规划；不自动部署生产或沿用旧停写确认。
